// LA VERSION, A LA VISTA EN EL MENU.
//
// Existe por un dia entero perdido: Pablo veia el juego viejo y no habia
// forma de saber por que. No era la cache del navegador: estaba entrando en
// un despliegue antiguo de Cloudflare (santa-perdida.pages.dev) donde la
// carpeta `arte/` no existe, asi que la portada no podia salir.
//
// Con el numero en la esquina se resuelve en dos segundos: si lo que pone en
// pantalla no es lo ultimo, es que esa no es la web buena o el navegador
// tiene los ficheros viejos. Sin esto hay que ir fichero a fichero con curl.
//
// SE SUBE A MANO al cerrar una tanda. No hay paso de compilacion que lo
// genere, y tampoco hace falta.
export const VERSION = 'v39 · 22-sep-2026';

// La unica direccion buena del juego. Si algun dia hay dudas otra vez.
export const WEB = 'santaperdida.creskio.com';
